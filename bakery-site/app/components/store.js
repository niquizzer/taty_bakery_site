
export default store = () => {
    reducer: {
        //todo
        switch (action, state) {
            case "ADD_ITEM":
                return {
                    //todo  
                };
            case "REMOVE_ITEM":
                return {
                    //todo
                };
            case "CHECKOUT":
                return {
                    //todo
                };
            default:
                return state;
        }
    }
}